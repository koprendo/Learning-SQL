-- in order to delete table, use DROP TABLE statement:
DROP TABLE test;

-- We can use it with IF EXISTS keywords:
DROP TABLE IF EXISTS test;
-- So, it doesn't return an error if there is not a table called test
