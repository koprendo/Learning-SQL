-- in order to list all the different values, use SELECT DISTINCT:
SELECT DISTINCT Continent FROM Country;

-- This will give the distinct rows where a and b, together, are different:
SELECT DISTINCT a, b FROM test;
